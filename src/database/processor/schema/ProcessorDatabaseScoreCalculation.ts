/**
 * Represents the score population progress in the processor's database.
 */
export interface ProcessorDatabaseScoreCalculation {
    readonly process_id: number;
    readonly score_id: number;
}
